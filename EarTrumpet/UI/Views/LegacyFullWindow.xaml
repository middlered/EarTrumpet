<Window x:Class="EarTrumpet.UI.Views.LegacyFullWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:Event="clr-namespace:EarTrumpet.Extensions.EventBinding"
        xmlns:Theme="clr-namespace:EarTrumpet.UI.Themes"
        xmlns:b="clr-namespace:EarTrumpet.UI.Behaviors"
        xmlns:ctl="clr-namespace:EarTrumpet.UI.Controls"
        xmlns:resx="clr-namespace:EarTrumpet.Properties"
        xmlns:views="clr-namespace:EarTrumpet.UI.Views"
        xmlns:vm="clr-namespace:EarTrumpet.UI.ViewModels"
        Name="WindowRoot"
        Title="{Binding Title}"
        MinWidth="{StaticResource WindowAndItemSize}"
        Theme:AcrylicBrush.Background="AcrylicColor_Settings"
        Theme:Options.Source="App"
        b:FrameworkElementEx.DisplaySettingsChanged="{Binding DisplaySettingsChanged}"
        b:FrameworkElementEx.FlowDirection="Auto"
        Background="Transparent"
        Closing="{Event:Binding OnClosing}"
        LocationChanged="{Event:Binding OnLocationChanged}"
        PreviewKeyDown="{Event:Binding OnPreviewKeyDown}"
        SizeChanged="{Event:Binding OnSizeChanged}"
        SizeToContent="WidthAndHeight"
        Style="{StaticResource DialogWindowStyle}"
        TextOptions.TextFormattingMode="Display"
        UseLayoutRounding="True"
        WindowStartupLocation="CenterOwner">
    <Window.Resources>
        <Style x:Key="LegacyVerticalSliderRepeatButtonStyle" TargetType="{x:Type RepeatButton}">
            <Setter Property="OverridesDefaultStyle" Value="True" />
            <Setter Property="IsTabStop" Value="False" />
            <Setter Property="Focusable" Value="False" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type RepeatButton}">
                        <Border Background="Transparent" />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="LegacyDeviceMenuItemStyle" TargetType="{x:Type MenuItem}">
            <Setter Property="MinHeight" Value="26" />
            <Setter Property="Padding" Value="12,4" />
            <Setter Property="FontSize" Value="12" />
        </Style>
        <Style x:Key="LegacyMixerFrameHeaderStyle" TargetType="{x:Type Border}">
            <Setter Property="Panel.ZIndex" Value="1" />
            <Setter Property="Padding" Value="7,2" />
            <Setter Property="HorizontalAlignment" Value="Left" />
            <Setter Property="VerticalAlignment" Value="Top" />
            <Setter Property="SnapsToDevicePixels" Value="True" />
            <Setter Property="Theme:Brush.Background" Value="FlyoutBackground" />
        </Style>
        <Style x:Key="LegacyVerticalVolumeSliderStyle" TargetType="{x:Type ctl:VerticalVolumeSlider}">
            <Setter Property="OverridesDefaultStyle" Value="True" />
            <Setter Property="Width" Value="28" />
            <Setter Property="Height" Value="124" />
            <Setter Property="Minimum" Value="0" />
            <Setter Property="Maximum" Value="100" />
            <Setter Property="SmallChange" Value="1" />
            <Setter Property="LargeChange" Value="10" />
            <Setter Property="IsTabStop" Value="False" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ctl:VerticalVolumeSlider}">
                        <Grid Width="28" Height="{TemplateBinding Height}">
                            <Border Width="2"
                                    Margin="0,7,0,7"
                                    HorizontalAlignment="Center"
                                    Theme:Brush.Background="Theme=Control{Theme}SliderTrackFillDisabled, HighContrast=ControlText"
                                    CornerRadius="1" />
                            <Border x:Name="VolumeFill"
                                    Width="2"
                                    Margin="0,7,0,7"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Bottom"
                                    Theme:Brush.Background="SystemAccent"
                                    CornerRadius="1" />
                            <Track x:Name="PART_Track"
                                   Margin="0,7,0,7"
                                   IsDirectionReversed="False"
                                   Orientation="Vertical">
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Command="Slider.DecreaseLarge"
                                                  Theme:Brush.Foreground="SystemAccent"
                                                  Style="{StaticResource LegacyVerticalSliderRepeatButtonStyle}" />
                                </Track.DecreaseRepeatButton>
                                <Track.Thumb>
                                    <Thumb x:Name="SliderThumb">
                                        <Thumb.Style>
                                            <Style BasedOn="{StaticResource SliderThumbStyle}" TargetType="{x:Type Thumb}">
                                                <Setter Property="Width" Value="20" />
                                                <Setter Property="Height" Value="{DynamicResource Mutable_SliderThumbWidth}" />
                                                <Setter Property="Theme:Brush.Foreground" Value="SystemAccent" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Path=IsMouseOver, RelativeSource={RelativeSource TemplatedParent}}" Value="True">
                                                        <Setter Property="Theme:Brush.Foreground" Value="Theme=Control{Theme}SliderThumbHover, HighContrast=Highlight" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Thumb.Style>
                                    </Thumb>
                                </Track.Thumb>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton Command="Slider.IncreaseLarge"
                                                  Style="{StaticResource LegacyVerticalSliderRepeatButtonStyle}" />
                                </Track.IncreaseRepeatButton>
                            </Track>
                            <Border x:Name="PeakMeter1"
                                    Width="2"
                                    Margin="12,7,0,7"
                                    HorizontalAlignment="Left"
                                    VerticalAlignment="Bottom"
                                    Theme:Brush.Background="Theme=ApplicationText{Theme}Theme, HighContrast=HotTrack" />
                            <Border x:Name="PeakMeter2"
                                    Width="2"
                                    Margin="14,7,0,7"
                                    HorizontalAlignment="Left"
                                    VerticalAlignment="Bottom"
                                    Theme:Brush.Background="Theme=ApplicationText{Theme}Theme, HighContrast=HotTrack" />
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="LegacyMixerColumnTitleTextStyle" TargetType="{x:Type TextBlock}">
            <Setter Property="Width" Value="88" />
            <Setter Property="Height" Value="36" />
            <Setter Property="Margin" Value="0,8,0,0" />
            <Setter Property="HorizontalAlignment" Value="Center" />
            <Setter Property="VerticalAlignment" Value="Top" />
            <Setter Property="TextAlignment" Value="Center" />
            <Setter Property="TextTrimming" Value="CharacterEllipsis" />
            <Setter Property="TextWrapping" Value="Wrap" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="Theme:Brush.Foreground" Value="Text" />
        </Style>
        <DataTemplate x:Key="LegacyAppColumnTemplate">
            <Grid Width="112" Height="256" Margin="0,8,0,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="140" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Grid Width="32"
                      Height="32"
                      HorizontalAlignment="Center">
                    <Border Width="32"
                            Height="32"
                            HorizontalAlignment="Center">
                        <Border.Style>
                            <Style TargetType="Border">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsMuted}" Value="true">
                                        <Setter Property="Opacity" Value="{StaticResource MutedOpacity}" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                        <Grid>
                            <ctl:ImageEx x:Name="LegacyAppIcon"
                                         Width="32"
                                         Height="32"
                                         SourceEx="{Binding}"
                                         Stretch="Fill" />
                            <Border>
                                <Border.Style>
                                    <Style TargetType="{x:Type Border}">
                                        <Setter Property="Visibility" Value="Collapsed" />
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Source, ElementName=LegacyAppIcon}" Value="{x:Null}">
                                                <Setter Property="Visibility" Value="Visible" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <TextBlock HorizontalAlignment="Center"
                                           VerticalAlignment="Center"
                                           Theme:Brush.Foreground="Text"
                                           FontSize="14"
                                           FontWeight="SemiBold"
                                           Text="{Binding IconText}" />
                            </Border>
                        </Grid>
                    </Border>
                </Grid>
                <TextBlock Grid.Row="1"
                           Style="{StaticResource LegacyMixerColumnTitleTextStyle}"
                           Text="{Binding DisplayName}"
                           ToolTipService.ToolTip="{Binding DisplayName}" />
                <ctl:VerticalVolumeSlider Grid.Row="2"
                                          Margin="0,8,0,8"
                                          HorizontalAlignment="Center"
                                          VerticalAlignment="Center"
                                          PeakValue1="{Binding PeakValue1}"
                                          PeakValue2="{Binding PeakValue2}"
                                          PlaySoundOnVolumeChange="{Binding IsSystemSoundsSession}"
                                          Style="{StaticResource LegacyVerticalVolumeSliderStyle}"
                                          Value="{Binding Volume, Mode=TwoWay}" />
                <Button Grid.Row="3"
                        Width="32"
                        Height="32"
                        Margin="0,0,0,8"
                        HorizontalAlignment="Center"
                        Click="LegacyAppMuteButton_Click"
                        ToolTip="{x:Static resx:Resources.ToggleMuteToolTip}">
                    <Button.Style>
                        <Style BasedOn="{StaticResource MuteButton}" TargetType="{x:Type Button}">
                            <Setter Property="Opacity" Value="1" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsMuted}" Value="true">
                                    <Setter Property="Opacity" Value="1" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                    <Button.Content>
                        <TextBlock Theme:Brush.Foreground="{Binding (Theme:Brush.Foreground), RelativeSource={RelativeSource AncestorType=Button}}"
                                   FontSize="16">
                            <TextBlock.Style>
                                <Style BasedOn="{StaticResource GlyphTextBlockStyle}" TargetType="{x:Type TextBlock}">
                                    <Setter Property="Text" Value="&#xE995;" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsMuted}" Value="True">
                                            <Setter Property="Text" Value="&#xE74F;" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </Button.Content>
                </Button>
            </Grid>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:DeviceViewModel}">
            <views:DeviceView Width="{StaticResource WindowAndItemSize}" Device="{Binding}" />
        </DataTemplate>
    </Window.Resources>
    <Grid>
        <Border Name="AcrylicBackgroundActive" Theme:Brush.Background="AcrylicBackgroundFallback" />
        <Border Name="AcrylicBackgroundWhileDragging">
            <Border.Style>
                <Style TargetType="{x:Type Border}">
                    <Setter Property="Theme:Brush.Background" Value="AcrylicBackgroundFallback" />
                    <Setter Property="Opacity" Value="0" />
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding ElementName=WindowRoot, Path=(Theme:AcrylicBrush.IsSuppressed)}" Value="True">
                            <DataTrigger.EnterActions>
                                <BeginStoryboard Name="IsSuppressed_Enter_AnimationsOn">
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                         From="0"
                                                         To="1"
                                                         Duration="00:00:00" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </DataTrigger.EnterActions>
                            <DataTrigger.ExitActions>
                                <BeginStoryboard Name="IsSuppressed_Exit_AnimationsOn">
                                    <Storyboard>
                                        <DoubleAnimationUsingKeyFrames BeginTime="0:0:0"
                                                                       Storyboard.TargetProperty="Opacity"
                                                                       Duration="0:00:00.25">
                                            <EasingDoubleKeyFrame KeyTime="0:0:0" Value="1" />
                                            <EasingDoubleKeyFrame KeyTime="0:0:0.1" Value="1" />
                                            <EasingDoubleKeyFrame KeyTime="0:0:0.25" Value="0" />
                                        </DoubleAnimationUsingKeyFrames>
                                    </Storyboard>
                                </BeginStoryboard>
                            </DataTrigger.ExitActions>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
        </Border>
        <Border Name="AcrylicBackgroundInactive" Visibility="Collapsed">
            <Border.Style>
                <Style TargetType="{x:Type Border}">
                    <Setter Property="Theme:Brush.Background" Value="AcrylicBackgroundFallback" />
                    <Setter Property="Opacity" Value="1" />
                    <Style.Triggers>
                        <MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding Source={StaticResource ThemeManager}, Path=AnimationsEnabled}" Value="True" />
                                <Condition Binding="{Binding ElementName=WindowRoot, Path=IsActive}" Value="True" />
                            </MultiDataTrigger.Conditions>
                            <MultiDataTrigger.EnterActions>
                                <StopStoryboard BeginStoryboardName="IsActive_Exit_AnimationsOff" />
                                <StopStoryboard BeginStoryboardName="IsActive_Enter_AnimationsOff" />
                                <BeginStoryboard Name="IsActive_Enter_AnimationsOn">
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                         From="1"
                                                         To="0"
                                                         Duration="00:00:00.15" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </MultiDataTrigger.EnterActions>
                            <MultiDataTrigger.ExitActions>
                                <StopStoryboard BeginStoryboardName="IsActive_Enter_AnimationsOff" />
                                <StopStoryboard BeginStoryboardName="IsActive_Exit_AnimationsOff" />
                                <BeginStoryboard Name="IsActive_Exit_AnimationsOn">
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                         From="0"
                                                         To="1"
                                                         Duration="00:00:00.15" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </MultiDataTrigger.ExitActions>
                        </MultiDataTrigger>
                        <MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding Source={StaticResource ThemeManager}, Path=AnimationsEnabled}" Value="False" />
                                <Condition Binding="{Binding ElementName=WindowRoot, Path=IsActive}" Value="True" />
                            </MultiDataTrigger.Conditions>
                            <MultiDataTrigger.EnterActions>
                                <StopStoryboard BeginStoryboardName="IsActive_Enter_AnimationsOn" />
                                <StopStoryboard BeginStoryboardName="IsActive_Exit_AnimationsOn" />
                                <BeginStoryboard Name="IsActive_Enter_AnimationsOff">
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                         From="1"
                                                         To="0"
                                                         Duration="00:00:00" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </MultiDataTrigger.EnterActions>
                            <MultiDataTrigger.ExitActions>
                                <StopStoryboard BeginStoryboardName="IsActive_Exit_AnimationsOn" />
                                <StopStoryboard BeginStoryboardName="IsActive_Enter_AnimationsOn" />
                                <BeginStoryboard Name="IsActive_Exit_AnimationsOff">
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                         From="0"
                                                         To="1"
                                                         Duration="00:00:00" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </MultiDataTrigger.ExitActions>
                        </MultiDataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
        </Border>
        <DockPanel x:Name="ContentRoot">
            <DockPanel DockPanel.Dock="Top">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Padding="12,8,12,8"
                               Text="{Binding ElementName=WindowRoot, Path=Title}"
                               TextAlignment="Left" />
                    <Button x:Name="MinimizeButton"
                            Grid.Column="1"
                            Style="{StaticResource MinimizeButton}" />
                    <Button x:Name="CloseButton"
                            Grid.Column="2"
                            Style="{StaticResource CloseButtonStyle}" />
                </Grid>
            </DockPanel>
            <Grid>
                <ScrollViewer x:Name="BaseVisual"
                              HorizontalScrollBarVisibility="Disabled"
                              VerticalScrollBarVisibility="Disabled">
                    <Grid Name="ContentGrid">
                        <Border Width="{StaticResource WindowAndItemSize}"
                                VerticalAlignment="Center"
                                Theme:Brush.Background="FlyoutBackground">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding AllDevices.Count}" Value="0">
                                            <Setter Property="Visibility" Value="Visible" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <TextBlock Margin="{StaticResource NoItemsPaneMargin}"
                                       HorizontalAlignment="Center"
                                       FontSize="{StaticResource {x:Static SystemFonts.MessageFontSizeKey}}"
                                       Text="{x:Static resx:Resources.NoDevicesPanelContent}"
                                       TextWrapping="Wrap" />
                        </Border>
                    <Grid x:Name="LegacyMixerLayout">
                        <Grid.Style>
                            <Style TargetType="Grid">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding AllDevices.Count}" Value="0">
                                        <Setter Property="Visibility" Value="Collapsed" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Grid.Style>
                        <Grid Margin="12"
                              MinWidth="452"
                              MinHeight="280">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="112" />
                                <ColumnDefinition Width="8" />
                                <ColumnDefinition Width="*" MinWidth="332" />
                            </Grid.ColumnDefinitions>
                            <Border Theme:Brush.Background="FlyoutBackground"
                                    Theme:Brush.BorderBrush="Light=LightChromeHigh, Dark=ControlDarkAppButtonTextDisabled/0.9, HighContrast=Text"
                                    BorderThickness="1,0,1,1">
                                <Grid MinHeight="280">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="*" />
                                </Grid.RowDefinitions>
                                <Grid Panel.ZIndex="1"
                                      VerticalAlignment="Top">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="7" />
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <Border Grid.Column="0"
                                            Height="1"
                                            VerticalAlignment="Top"
                                            Theme:Brush.Background="Light=LightChromeHigh, Dark=ControlDarkAppButtonTextDisabled/0.9, HighContrast=Text" />
                                    <Border Grid.Column="2"
                                            Height="1"
                                            VerticalAlignment="Top"
                                            Theme:Brush.Background="Light=LightChromeHigh, Dark=ControlDarkAppButtonTextDisabled/0.9, HighContrast=Text" />
                                    <Border Grid.Column="1"
                                            Margin="0,-10,0,0"
                                            Style="{StaticResource LegacyMixerFrameHeaderStyle}">
                                        <TextBlock Theme:Brush.Foreground="Text"
                                                   FontSize="12"
                                                   Text="{x:Static resx:Resources.VolumeMixerDeviceHeaderText}" />
                                    </Border>
                                </Grid>
                                <Grid Grid.Row="1"
                                      Width="110"
                                      Height="256"
                                      Margin="0,8,0,0"
                                      HorizontalAlignment="Center"
                                      DataContext="{Binding DefaultDevice}">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="140" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>
                                    <Grid Width="32"
                                          Height="32"
                                          HorizontalAlignment="Center">
                                        <ctl:ImageEx x:Name="LegacyDeviceIcon"
                                                     Width="32"
                                                     Height="32"
                                                     SourceEx="{Binding}"
                                                     Stretch="Uniform" />
                                        <TextBlock Width="32"
                                                   Height="32"
                                                   HorizontalAlignment="Center"
                                                   FontSize="30">
                                            <TextBlock.Style>
                                                <Style BasedOn="{StaticResource GlyphTextBlockStyle}" TargetType="{x:Type TextBlock}">
                                                    <Setter Property="Text" Value="&#xE995;" />
                                                    <Setter Property="Visibility" Value="Collapsed" />
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Source, ElementName=LegacyDeviceIcon}" Value="{x:Null}">
                                                            <Setter Property="Visibility" Value="Visible" />
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding IconKind}" Value="Mute">
                                                            <Setter Property="Text" Value="&#xE74F;" />
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding IconKind}" Value="Microphone">
                                                            <Setter Property="Text" Value="&#xE720;" />
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding IconKind}" Value="Bar0">
                                                            <Setter Property="Text" Value="&#xE992;" />
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding IconKind}" Value="Bar1">
                                                            <Setter Property="Text" Value="&#xE993;" />
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding IconKind}" Value="Bar2">
                                                            <Setter Property="Text" Value="&#xE994;" />
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding IconKind}" Value="Bar3">
                                                            <Setter Property="Text" Value="&#xE995;" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </Grid>
                                    <Grid Grid.Row="1"
                                          Width="98"
                                          Height="36"
                                          Margin="0,8,0,0"
                                          HorizontalAlignment="Center">
                                        <TextBlock Text="{Binding DataContext.DefaultDeviceShortName, ElementName=WindowRoot}"
                                                   ToolTipService.ToolTip="{Binding DisplayName}">
                                            <TextBlock.Style>
                                                <Style BasedOn="{StaticResource LegacyMixerColumnTitleTextStyle}" TargetType="{x:Type TextBlock}">
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding DataContext.HasMultipleDevices, ElementName=WindowRoot}" Value="True">
                                                            <Setter Property="Visibility" Value="Collapsed" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                        <Button x:Name="LegacyDeviceSelectorButton"
                                                Height="24"
                                                HorizontalAlignment="Stretch"
                                                VerticalAlignment="Top"
                                                Click="LegacyDeviceSelectorButton_Click"
                                                ToolTip="{Binding DataContext.DefaultDevice.DisplayName, ElementName=WindowRoot}">
                                            <Button.Style>
                                                <Style BasedOn="{StaticResource InvisibleButtonStyle}" TargetType="{x:Type Button}">
                                                    <Setter Property="Visibility" Value="Collapsed" />
                                                    <Setter Property="Padding" Value="4,0" />
                                                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding DataContext.HasMultipleDevices, ElementName=WindowRoot}" Value="True">
                                                            <Setter Property="Visibility" Value="Visible" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Button.Style>
                                            <Button.ContextMenu>
                                                <ContextMenu ItemContainerTemplateSelector="{x:Null}"
                                                             Padding="2,3"
                                                             Placement="Bottom"
                                                             UsesItemContainerTemplate="False" />
                                            </Button.ContextMenu>
                                            <Grid>
                                                <TextBlock Width="76"
                                                           HorizontalAlignment="Center"
                                                           VerticalAlignment="Center"
                                                           FontSize="12"
                                                           Text="{Binding DataContext.DefaultDeviceShortName, ElementName=WindowRoot}"
                                                           TextAlignment="Center"
                                                           TextTrimming="CharacterEllipsis"
                                                           TextWrapping="NoWrap" />
                                                <TextBlock Margin="0,0,4,0"
                                                           HorizontalAlignment="Right"
                                                           VerticalAlignment="Center"
                                                           FontSize="9"
                                                           Style="{StaticResource GlyphTextBlockStyle}"
                                                           Text="&#xE70D;" />
                                            </Grid>
                                        </Button>
                                    </Grid>
                                    <ctl:VerticalVolumeSlider Grid.Row="2"
                                                              Margin="0,8,0,8"
                                                              HorizontalAlignment="Center"
                                                              VerticalAlignment="Center"
                                                              PeakValue1="{Binding PeakValue1}"
                                                              PeakValue2="{Binding PeakValue2}"
                                                              PlaySoundOnVolumeChange="True"
                                                              Style="{StaticResource LegacyVerticalVolumeSliderStyle}"
                                                              Value="{Binding Volume, Mode=TwoWay}" />
                                    <Button Grid.Row="3"
                                            Width="32"
                                            Height="32"
                                            Margin="0,0,0,8"
                                            HorizontalAlignment="Center"
                                            Command="{Binding DataContext.ToggleLegacyMasterMute, ElementName=WindowRoot}"
                                            ToolTip="{x:Static resx:Resources.ToggleMuteToolTip}">
                                        <Button.Style>
                                            <Style BasedOn="{StaticResource MuteButton}" TargetType="{x:Type Button}">
                                                <Setter Property="Opacity" Value="1" />
                                            </Style>
                                        </Button.Style>
                                        <Button.Content>
                                            <TextBlock Theme:Brush.Foreground="{Binding (Theme:Brush.Foreground), RelativeSource={RelativeSource AncestorType=Button}}"
                                                       FontSize="16">
                                                <TextBlock.Style>
                                                    <Style BasedOn="{StaticResource GlyphTextBlockStyle}" TargetType="{x:Type TextBlock}">
                                                        <Setter Property="Text" Value="&#xE995;" />
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding IsMuted}" Value="True">
                                                                <Setter Property="Text" Value="&#xE74F;" />
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </TextBlock.Style>
                                            </TextBlock>
                                        </Button.Content>
                                    </Button>
                                </Grid>

                            </Grid>
                            </Border>

                            <Border Grid.Column="2"
                                    Theme:Brush.Background="FlyoutBackground"
                                    Theme:Brush.BorderBrush="Light=LightChromeHigh, Dark=ControlDarkAppButtonTextDisabled/0.9, HighContrast=Text"
                                    BorderThickness="1,0,1,1">
                                <Grid MinHeight="280">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="*" />
                                    </Grid.RowDefinitions>
                                    <Grid Panel.ZIndex="1"
                                          VerticalAlignment="Top">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="11" />
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <Border Grid.Column="0"
                                                Height="1"
                                                VerticalAlignment="Top"
                                                Theme:Brush.Background="Light=LightChromeHigh, Dark=ControlDarkAppButtonTextDisabled/0.9, HighContrast=Text" />
                                        <Border Grid.Column="2"
                                                Height="1"
                                                VerticalAlignment="Top"
                                                Theme:Brush.Background="Light=LightChromeHigh, Dark=ControlDarkAppButtonTextDisabled/0.9, HighContrast=Text" />
                                        <Border Grid.Column="1"
                                                Margin="0,-10,0,0"
                                                Style="{StaticResource LegacyMixerFrameHeaderStyle}">
                                            <TextBlock Theme:Brush.Foreground="Text"
                                                       FontSize="12"
                                                       Text="{x:Static resx:Resources.VolumeMixerApplicationsHeaderText}" />
                                        </Border>
                                    </Grid>
                                    <ScrollViewer Grid.Row="1"
                                                  HorizontalScrollBarVisibility="Auto"
                                                  VerticalScrollBarVisibility="Disabled">
                                        <ItemsControl ItemTemplate="{StaticResource LegacyAppColumnTemplate}"
                                                      ItemsSource="{Binding DefaultDevice.Apps}">
                                            <ItemsControl.ItemsPanel>
                                                <ItemsPanelTemplate>
                                                    <StackPanel Orientation="Horizontal" />
                                                </ItemsPanelTemplate>
                                            </ItemsControl.ItemsPanel>
                                        </ItemsControl>
                                    </ScrollViewer>
                                </Grid>
                            </Border>
                        </Grid>
                    </Grid>
                </Grid>
            </ScrollViewer>
                <ctl:AppPopup IsOpen="{Binding Dialog.IsVisible, Mode=TwoWay}"
                              PlacementTarget="{Binding Dialog.Source}"
                              Style="{StaticResource AppPopup}">
                    <ContentControl Content="{Binding Dialog.Focused}" />
                </ctl:AppPopup>
            </Grid>
        </DockPanel>
        <Border Name="LightDismissBorder"
                Theme:Brush.Background="Background"
                PreviewMouseDown="{Event:Binding OnLightDismissBorderPreviewMouseDown}">
            <Border.Style>
                <Style TargetType="{x:Type Border}">
                    <Setter Property="Visibility" Value="Collapsed" />
                    <Style.Triggers>
                        <MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding Dialog.IsVisible}" Value="True" />
                                <Condition Binding="{Binding Source={StaticResource ThemeManager}, Path=AnimationsEnabled}" Value="True" />
                            </MultiDataTrigger.Conditions>
                            <MultiDataTrigger.EnterActions>
                                <StopStoryboard BeginStoryboardName="EnterLightDismiss_AnimationsOff" />
                                <StopStoryboard BeginStoryboardName="ExitLightDismiss_AnimationsOff" />
                                <BeginStoryboard Name="EnterLightDismiss_AnimationsOn">
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                         From="0"
                                                         To="1"
                                                         Duration="00:00:00.15" />
                                        <ObjectAnimationUsingKeyFrames BeginTime="00:00:00" Storyboard.TargetProperty="(UIElement.Visibility)">
                                            <DiscreteObjectKeyFrame KeyTime="00:00:00" Value="{x:Static Visibility.Visible}" />
                                        </ObjectAnimationUsingKeyFrames>
                                    </Storyboard>
                                </BeginStoryboard>
                            </MultiDataTrigger.EnterActions>
                            <MultiDataTrigger.ExitActions>
                                <StopStoryboard BeginStoryboardName="EnterLightDismiss_AnimationsOff" />
                                <StopStoryboard BeginStoryboardName="ExitLightDismiss_AnimationsOff" />
                                <BeginStoryboard Name="ExitLightDismiss_AnimationsOn">
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                         From="1"
                                                         To="0"
                                                         Duration="00:00:00.15" />
                                        <ObjectAnimationUsingKeyFrames BeginTime="00:00:00" Storyboard.TargetProperty="(UIElement.Visibility)">
                                            <DiscreteObjectKeyFrame KeyTime="00:00:00.15" Value="{x:Static Visibility.Collapsed}" />
                                        </ObjectAnimationUsingKeyFrames>
                                    </Storyboard>
                                </BeginStoryboard>
                            </MultiDataTrigger.ExitActions>
                        </MultiDataTrigger>

                        <MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding Dialog.IsVisible}" Value="True" />
                                <Condition Binding="{Binding Source={StaticResource ThemeManager}, Path=AnimationsEnabled}" Value="False" />
                            </MultiDataTrigger.Conditions>
                            <MultiDataTrigger.EnterActions>
                                <StopStoryboard BeginStoryboardName="EnterLightDismiss_AnimationsOn" />
                                <StopStoryboard BeginStoryboardName="ExitLightDismiss_AnimationsOn" />
                                <BeginStoryboard Name="EnterLightDismiss_AnimationsOff">
                                    <Storyboard>
                                        <ObjectAnimationUsingKeyFrames BeginTime="00:00:00" Storyboard.TargetProperty="(UIElement.Visibility)">
                                            <DiscreteObjectKeyFrame KeyTime="00:00:00" Value="{x:Static Visibility.Visible}" />
                                        </ObjectAnimationUsingKeyFrames>
                                    </Storyboard>
                                </BeginStoryboard>
                            </MultiDataTrigger.EnterActions>
                            <MultiDataTrigger.ExitActions>
                                <StopStoryboard BeginStoryboardName="EnterLightDismiss_AnimationsOn" />
                                <StopStoryboard BeginStoryboardName="ExitLightDismiss_AnimationsOn" />
                                <BeginStoryboard Name="ExitLightDismiss_AnimationsOff">
                                    <Storyboard>
                                        <ObjectAnimationUsingKeyFrames BeginTime="00:00:00" Storyboard.TargetProperty="(UIElement.Visibility)">
                                            <DiscreteObjectKeyFrame KeyTime="00:00:00" Value="{x:Static Visibility.Collapsed}" />
                                        </ObjectAnimationUsingKeyFrames>
                                    </Storyboard>
                                </BeginStoryboard>
                            </MultiDataTrigger.ExitActions>
                        </MultiDataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
            <Border Margin="0,0,0,14" Opacity="0.8">
                <Border.Effect>
                    <BlurEffect KernelType="Gaussian"
                                Radius="{StaticResource WindowBlurRadius}"
                                RenderingBias="Performance" />
                </Border.Effect>
                <Border.Background>
                    <VisualBrush AlignmentX="Left"
                                 AlignmentY="Top"
                                 Stretch="UniformToFill"
                                 Visual="{Binding ElementName=ContentRoot}" />
                </Border.Background>
            </Border>
        </Border>
    </Grid>
</Window>
